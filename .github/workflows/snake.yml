name: Generate Snake Animation

on:
  # 每天 UTC 时间 0 点自动运行
  schedule:
    - cron: "0 0 * * *"
  
  # 允许手动触发
  workflow_dispatch:
  
  # 推送到主分支时也运行
  push:
    branches:
      - main

permissions:
  contents: write  # 必须有写入权限

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出当前仓库
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: main  # 明确指定分支

      # 2. 生成贪吃蛇图像
      - name: Generate Snake Graphics
        uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: OrionGu1  # 你的用户名
          # 生成多种格式和颜色
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark
            dist/github-snake.gif?color_snake=orange&color_dots=#bfd6f6,#8dbdff,#64a1f4,#4b91f1,#3c7fda
          gif_out_path: dist/github-snake.gif
          svg_out_path: dist/github-snake.svg
          dark_svg_out_path: dist/github-snake-dark.svg

      # 3. 上传生成的文件到 artifact（便于调试）
      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        if: success()
        with:
          name: snake-output
          path: dist/
          retention-days: 1

      # 4. 部署到独立的 gh-pages 分支（推荐）
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        if: success()
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages  # 使用独立的 gh-pages 分支
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          commit_message: '🔄 Update snake animation - $(date)'
